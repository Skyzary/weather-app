services:
  weather-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: weather-app
    ports:
      - "8080:80"
    restart: always  
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro  